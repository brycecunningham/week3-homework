<ul>
  <% @places.each do |place| %>
    <li>
      <p>
        <%= link_to place.title, "/places/#{place.id}" %>
        (<%= place.price %>) <%= link_to "(delete)", "/places/#{place.id}/delete" %>
      </p>
    </li>
  <% end %>
</ul>